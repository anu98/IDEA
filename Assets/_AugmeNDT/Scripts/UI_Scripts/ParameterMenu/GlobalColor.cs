using System.Collections.Generic;
using UnityEngine;

namespace AugmeNDT
{
    public static class GlobalColor
    {
        // Global default, used by all Vis when they initialise
        public static Color[] CurrentScheme = {
            Color.cyan, Color.white, Color.magenta
        };
    }
}
